# ============================================
# Environment Variables Configuration
# ============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control (it's in .gitignore)

# ============================================
# Site Configuration
# ============================================

# Site URL - Used for sitemap generation and canonical URLs
# Example: https://www.yourfuneralhome.com
SITE_URL=https://example.com

# ============================================
# Email Configuration (SMTP)
# ============================================
# These variables are used when you replace the mock email transporter
# with a real SMTP implementation in src/pages/api/contact.ts
#
# To enable email sending:
# 1. Install an email library (e.g., nodemailer or resend)
# 2. Uncomment and configure the SMTP settings below
# 3. Update src/pages/api/contact.ts to use these variables

# SMTP Server Host
# Example: smtp.gmail.com, smtp.sendgrid.net, smtp.resend.com
SMTP_HOST=smtp.example.com

# SMTP Server Port
# Common ports: 587 (TLS), 465 (SSL), 25 (unencrypted, not recommended)
SMTP_PORT=587

# SMTP Authentication Username
# Usually your email address or API username
SMTP_USER=your-email@example.com

# SMTP Authentication Password
# Your email password or API key
SMTP_PASS=your-password-or-api-key

# Email From Address
# The email address that will appear as the sender
# This can also be configured in src/config/siteDetails.ts
EMAIL_FROM=noreply@example.com

# Email To Address (Optional)
# The recipient email for contact form submissions
# If not set, defaults to EMAIL_FROM
EMAIL_TO=contact@example.com

# ============================================
# Contact Form API Configuration (Optional)
# ============================================

# Rate Limiting - Maximum requests per window (default: 5)
# Adjust if you need different rate limiting behavior
RATE_LIMIT_MAX_REQUESTS=5

# Rate Limiting - Window duration in minutes (default: 15)
# Time window for rate limiting
RATE_LIMIT_WINDOW_MINUTES=15

# ============================================
# Development/Production Flags (Optional)
# ============================================

# Environment Mode
# Options: development, production, preview
# Used to determine which configuration to use
NODE_ENV=development

# ============================================
# Notes
# ============================================
# - All environment variables are optional except SITE_URL (recommended)
# - The contact form currently uses a mock email transporter
# - To enable real email sending, configure SMTP variables above
# - See src/pages/api/contact.ts for email implementation details
# - See src/config/siteDetails.ts for site-specific configuration
